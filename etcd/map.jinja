{% set server = salt['grains.filter_by']({
    'Debian': {
        'pkgs': ['python-etcd'],
        'services': ['etcd']
    },
    'RedHat': {
        'pkgs': [],
        'services': []
    },
}, merge=salt['pillar.get']('etcd:server')) %}
